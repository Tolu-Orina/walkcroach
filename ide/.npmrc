# Same rationale as cli/.npmrc — `npm version` tags `ide-v0.2.1` so it matches
# the `ide-v*` trigger in publish-ide.yml. The extension is not an npm package;
# npm is used here only as the version/tag tool.
tag-version-prefix=ide-v
