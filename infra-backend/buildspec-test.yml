version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
  pre_build:
    commands:
      - echo "Backend unit / typecheck"
      - node -v
      - npm -v
  build:
    commands:
      - cd infra-backend
      - npm ci
      # Root typecheck builds packages (dist/) then runs tsc in each workspace
      - npm run typecheck
      - npm run test
artifacts:
  files:
    - '**/*'
  name: backend-unit-test
