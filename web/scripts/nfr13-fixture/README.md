# NFR-13 generated-app fixture

Minimal Vite app mirroring `web/src/templates/scaffold.ts` (`lib/db.ts`, `lib/walkcroach.ts`).

Used by `scripts/nfr13-secret-leak-scan.mjs` to build a production client bundle and assert no secrets or credentials are inlined.
