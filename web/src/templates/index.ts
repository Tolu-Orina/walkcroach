/**
 * Re-export shim (C3.1) — see `scaffold.ts` for why.
 *
 * Definitions live in `@walkcroach/templates`, shared with the CLI.
 */
export {
  TEMPLATES,
  DEFAULT_TEMPLATE_ID,
  getTemplate,
} from '@walkcroach/templates';
export type { TemplateDefinition } from '@walkcroach/templates';
